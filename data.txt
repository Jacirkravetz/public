# Start the timer
$timer = [System.Diagnostics.Stopwatch]::StartNew()

Write-Host "⏳ Starting the process..." -ForegroundColor Cyan

# Simulate a long-running process (e.g., 5 seconds)
Start-Sleep -Seconds 5

# Example placeholder for your actual processing:
# $connection.Open()
# $reader = $command.ExecuteReader()
# while ($reader.Read()) { ... }
# $reader.Close()
# $connection.Close()

# Stop the timer
$timer.Stop()

# Calculate elapsed time
$elapsed = $timer.Elapsed
$hours = [int]$elapsed.TotalHours
$minutes = $elapsed.Minutes
$seconds = $elapsed.Seconds

Write-Host "`n✅ Process completed!" -ForegroundColor Green
Write-Host "⏱ Total elapsed time: $hours hour(s), $minutes minute(s), $seconds second(s)" -ForegroundColor Yellow
