 // Obtenha a URL de logout do Azure AD
    var azureAdLogoutUrl = "https://login.microsoftonline.com/{tenantId}/oauth2/v2.0/logout";
    var postLogoutRedirectUri = "https://localhost:5001"; // Substitua pela URL do seu app

    // Limpar cookies de autenticação
    await context.SignOutAsync();
    context.Response.Redirect($"{azureAdLogoutUrl}?post_logout_redirect_uri={postLogoutRedirectUri}");
