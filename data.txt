 using (var privateKeyStream = new MemoryStream(System.Text.Encoding.UTF8.GetBytes(_config.PrivateKey)))
        {
            var keyFile = new PrivateKeyFile(privateKeyStream);
            var authMethod = new PrivateKeyAuthenticationMethod(_config.Username, keyFile);
            var connectionInfo = new ConnectionInfo(_config.ServerAddress, _config.Port, _config.Username, authMethod);

            using (var client = new SftpClient(connectionInfo))
            {
