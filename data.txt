event Action<Message, SessionID>? MessageReceived;

    public event Action<Message, SessionID>? MessageReceived;



_handler.MessageReceived += (message, sessionID) =>
        {
            MessageReceived?.Invoke(message, sessionID);
        };



 // Aqui você assina o evento para receber as mensagens
        sessionManager.MessageReceived += (message, sessionID) =>
        {
            Console.WriteLine($"Received message from session {sessionID}: {message}");
            // Faça o push para outras partes do sistema, tratamento, etc
        };

        await sessionManager.StartAsync(CancellationToken.None);
