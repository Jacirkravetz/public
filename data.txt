WITH Numeros AS (
    SELECT TOP 10 ROW_NUMBER() OVER (ORDER BY (SELECT NULL)) AS N
    FROM sys.all_objects
)
SELECT CAST(RAND(CHECKSUM(NEWID())) * (100000000 - 100000) + 100000 AS BIGINT) AS NumeroAleatorio
FROM Numeros;
