 using var reader = await connection.ExecuteReaderAsync("SELECT Id, Nome FROM Usuarios", commandTimeout: 0); 

    while (await reader.ReadAsync())
    {
        yield return new Usuario
        {
            Id = reader.GetInt32(0),
            Nome = reader.GetString(1)
        };
    }
