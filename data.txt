public class StringSerializer : ISerializer<object>
{
    public byte[] Serialize(object data, SerializationContext context)
    {
        return data == null ? null : System.Text.Encoding.UTF8.GetBytes(data.ToString());
    }
}



      SchemaType.String => new ProducerBuilder<string, object>(producerConfig)
                .SetKeySerializer(Serializers.Utf8)
                .SetValueSerializer(new StringSerializer())
                .Build(),
