public static string ConvertXDocumentToString(XDocument xDoc, bool indent = true)
    {
        var settings = new SaveOptions();
        settings = indent ? SaveOptions.None : SaveOptions.DisableFormatting;
        return xDoc.ToString(settings);
    }
