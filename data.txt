// Act & Assert
        var exception = Assert.Throws<ArgumentException>(() =>
        {
            XmlRecursiveExtractor.ExtractSections(xml, "Pedido", includedSections);
        });

        Assert.Equal("XML input cannot be null or empty.", exception.Message);
