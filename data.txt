Status = 
VAR Hoje = TODAY() 
RETURN
    SWITCH(
        TRUE(),
        'Tabela'[Fim] < Hoje, "Finalizado",
        'Tabela'[Inicio] > Hoje, "Futura",
        'Tabela'[Inicio] <= Hoje && 'Tabela'[Fim] >= Hoje, "Em progresso",
        "Sem Informação"
    )
