StatusVencimento = 
VAR DataValida = 'Tabela'[DataFormatada]
RETURN
IF (
    NOT ISBLANK(DataValida),
    IF (DataValida < TODAY(), "Vencido", "A vencer"),
    BLANK() -- ou "Data inválida", se preferir
)
