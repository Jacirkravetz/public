
SELECT TOP 1 Resultado
FROM (
    SELECT Nome AS Resultado
    FROM MinhaTabela
    WHERE Nome = @Valor1

    UNION ALL

    SELECT @Valor2
    WHERE NOT EXISTS (SELECT 1 FROM MinhaTabela WHERE Nome = @Valor1)
) AS ResultadoFinal;


DECLARE @Valor1 NVARCHAR(100) = 'João';
DECLARE @Valor2 NVARCHAR(100) = 'Valor Padrão';

SELECT ISNULL(
    (SELECT TOP 1 Nome FROM MinhaTabela WHERE Nome = @Valor1),
    @Valor2
) AS Resultado;




DECLARE @Valor1 NVARCHAR(100) = 'João';
DECLARE @Valor2 NVARCHAR(100) = 'Valor Padrão';

SELECT COALESCE(
    (SELECT TOP 1 Nome FROM MinhaTabela WHERE Nome = @Valor1),
    @Valor2
) AS Resultado;
